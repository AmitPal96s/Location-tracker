# Location-tracker
Live Location Tracker is a real-time web app that lets users share and view their live location on an interactive map. It uses the browser’s Geolocation API and Socket.IO to instantly sync positions between multiple users without refreshing the page.
